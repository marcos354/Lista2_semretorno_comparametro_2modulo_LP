# Digite aqui seu código
def reajuste(preco):
   valorFinal = preco + (preco * 0.09)
   print("O preço do produto com reajuste de 9% é: {:.2f}".format(valorFinal))
def main():
  valor = float(input("Informe o valor do produto: "))
  reajuste(valor)
main()
